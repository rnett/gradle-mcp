## 1. Skill Setup

- [x] 1.1 Create directory `skills/gradle_expert/` and `skills/gradle_expert/references/`.
- [x] 1.2 Create `skills/gradle_expert/SKILL.md` with basic metadata and constitution.

## 2. Best Practices Reference

- [x] 2.1 Author `skills/gradle_expert/references/best_practices.md` with guidelines on Kotlin DSL and performance.
- [x] 2.2 Author `skills/gradle_expert/references/common_build_patterns.md` with examples of multi-project setup and standard conventions.
- [x] 2.3 Author `skills/gradle_expert/references/internal_research_guidelines.md` with instructions on using `gradle_docs` and `search_dependency_sources` for Gradle engine and plugin research.

## 3. Skill Implementation

- [x] 3.1 Define "Directives" in `skills/gradle_expert/SKILL.md` for build authoring and performance audits.
- [x] 3.2 Define "Workflows" in `skills/gradle_expert/SKILL.md` for creating modules and managing dependencies.
- [x] 3.3 Add "Examples" for common tasks like adding a dependency or a new module.

## 4. Verification

- [x] 4.1 Verify the skill content by reading it back and checking it against the `gradle-mcp` project structure.
- [x] 4.2 Run `./gradlew check` to ensure overall project health.
